#include <bits/stdc++.h>
using namespace std;

void solve(){
    int n;
    cin >> n;
    int x, y, z; cin >> x >> y >> z;
    cout << (int)'A' << " " << (int)'Z';
}
int main(){
    ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    solve();
    return 0;
}